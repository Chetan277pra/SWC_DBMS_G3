
CREATE TABLE employees1
(
    empid INT PRIMARY KEY,
    ename VARCHAR(50),
    salary INT,
    managerid INT
);

INSERT INTO employees1
VALUES
(1, 'AMAN', 50000, NULL),
(2, 'SHREYA', 40000, 1),
(3, 'PIYUSH', 70000, 1),
(4, 'NEHA', 55000, NULL),
(5, 'NITIKA', 65000, 4),
(6, 'MANISH', 50000, 4);

SELECT E.empid , E.ename FROM employees1 E
where salary > (
SELECT salary from employees1 e2 WHERE empid = E.managerid
)
