SELECT COUNT(*)/2 AS payment_count
FROM transactions T1
 JOIN transactions T2 ON T1.credit_card_id = T2.credit_card_id
 WHERE(
T1.merchant_id = T2.merchant_id AND T1.amount = T2.amount AND T2.transaction_timestamp - T1.transaction_timestamp < INTERVAL '10 minutes'
 );
